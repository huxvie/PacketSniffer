PacketSniffer is a powerful mitm-proxy inspired by Proxyman. It supports real-time SSL decryption and websocket interceptions

built with tauri

for contributors:
bun dev: starts dev env
bun run build: builds a binary

ill fix readme later
